import React from 'react';
import EyeIcon from 'ps-react-ng/EyeIcon';

/** TODO: For icons, consider writing a script to generate a static page of icons. Then weave that into your doc generation */
export default function Example() {
    return <EyeIcon />;
}